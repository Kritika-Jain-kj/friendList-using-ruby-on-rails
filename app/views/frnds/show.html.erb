
<%= render @frnd %>

<div>
  <div class="btn-group me-2" role="group" aria-label="First group">
    <%= link_to "Edit", edit_frnd_path(@frnd), class:"btn btn-secondary" %>
  </div>
  <div class="btn-group me-2" role="group" aria-label="Second group"> 
    <%= button_to "Delete", @frnd, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-outline-danger btn-sm" %>
  </div>
  <%= link_to "Back", frnds_path, class:"btn btn-secondary" %>

</div>
