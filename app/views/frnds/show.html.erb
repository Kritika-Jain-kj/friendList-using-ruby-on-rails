
<%= render @frnd %>

<div>
  <%= link_to "Edit", edit_frnd_path(@frnd), class:"btn btn-secondary" %> 
  <%= link_to "Delete", @frnd, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-outline-danger btn-sm" %>
  <%= link_to "Back", frnds_path, class:"btn btn-secondary" %>

</div>
